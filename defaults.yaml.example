# User Preferences Configuration
# This file stores your preferred default models for each provider.
# Run 'ask --configure' to update these settings interactively.

# Default models per provider (will use first available if not set)
defaults:
  gemini: gemini-2.5-flash
  claude: claude-3-5-sonnet-20241022
  chatgpt: gpt-4o
  deepseek: deepseek-chat
